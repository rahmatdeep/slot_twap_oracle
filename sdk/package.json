{
  "name": "@slot-twap-oracle/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the Slot TWAP Oracle Solana program",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "slot-twap-oracle": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "solana",
    "oracle",
    "twap",
    "anchor"
  ],
  "repository": {
    "type": "git",
    "directory": "sdk"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/web3.js": "^1.98.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  }
}
